
# MySQL / JPA[Hibernate] Configuration
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/grow_api
    username: root
    password: Dlsgud*132
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true


# Logging Application
logging:
  level:
    org.hibernate.type.descriptor.sql: trace